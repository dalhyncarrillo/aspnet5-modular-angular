@{
    ViewBag.Title = "Sensitech Plugin Demo";
}

<h2>Email: @ViewBag.Email</h2>

<ul id="module-list">
    @foreach(var module in ViewBag.Modules){
        <li>@module.Name</li>
    }
</ul>
